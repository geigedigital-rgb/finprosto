[build]
builder = "NIXPACKS"
buildCommand = "npm run build"
watchPatterns = ["src/**", "server/**", "index.html", "package.json"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
